% 192372293 - mahesh k
% Pattern Matching Example

match([a, b, c]).
match([a, X, c]) :- write('Matched pattern with variable X = '), write(X), nl.

:- initialization(main).

main :-
    write('192372293 - mahesh k'), nl,
    match([a, b, c]).
